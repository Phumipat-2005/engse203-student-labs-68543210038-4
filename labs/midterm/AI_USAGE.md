# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นายภูมิพัฒน์ วงศ์ดาว **รหัส:** 68543210038-4 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 13:55 | B1.1 | error ใน console มันเเสดงถึงอะไรมันบอก error ใน SummaryPanel.jsx : 12 | จากรูปมีการกำหนดค่า key ไว้ด้านบน คือ item เเต่ยังไม่ได้นำมาใส่ | ใส่ค่า key ลงใน article |
| 14:30 | B1.2 | เเถบเเสดง status มันไม่ตรงกับรายการคำร้องด้านล่างปัญหานี้เกิดจากอะไร | ให้ไปเช็ค summary ในไฟล์ DashboardPage.jsx ว่าได้ใส่ค่าไว้ใน [] ไหมหรือเป็นค่าว่าง | ลองไปเช็คดูพบว่าไม่มีค่าอยู่ใน [] จริงเเละ completed มีการเขียนผิดด้วย |
| 15:03 | B1.3 | กด filter เเล้วมันไม่กรองรายการคำร้องต้องไปเเก้ไขยังไง | ให้ไปเช็คใน DashboardPage เหมือนเดิมเเต่ให้ไปดูตรง statusfilter ว่าไม่ได้ตั้งค่าล็อคอะไรไว้ใช่ไหม | ไปเช็คไฟล์ DashboardPage เเละลองไปดู statusfilter ดู |
| 15:23 | B1.4 | พิมพ์ url เเล้วมันไม่เเสดงรายการออกมา เช่น /#/requests/REQ-102 | ให้ไปเช็คในไฟล์ App ว่า rount เส้นทางมีชื่อว่าอะไร เเล้วไปดูใน RequestDetailPage ต่อว่าชื่อมันตรงกันไหม | ไปลองเช็คตามไฟล์ที่บอกเเล้วพบว่าใน useEffect ไม่ได้ใส่ค่าใน [] |
| 15:52 | B1.5-6 | กดลบเเล้วหน้าขาวต้อง f5 เพื่อที่จะกลับมาเเสดง Dashboard เหมือนเดิมต้องไปเเก้ไขที่ไหน | ไปเช็คตัว handleDelete ใน DashboardPage | ไปเช็คตามตำเเหน่งที่บอก|
| 16:15 | B2.1 | ต้องการเพิ่มช่องค้นหาเหนือรายการคำร้องต้องเเก้ไขหรือเพิ่มตรงไหนบ้าง | สามารถเพิ่มได้ใน DashboardPage | เพิ่มเนื้อหาด้านล่างตรง TODO B2 ที่มีทั้งด้านบนเเละด้านล่างใน DashboardPage |
| 16:35 | B2.2-3 | ต้องการเพิ่มระบบกรองรายการต้องเพิ่มหรือเเก้ไขอะไรเพิ่มบ้าง | ไปเเก้ไขตรง filteredRequests  | ไปเพิ่มเงื่อนไขของสถานะ |
| 16:50 | B2.4 | ต้องการเพิ่มถ้าค้นเเล้วไม่เจอ ให้เเสดงข้อความ ไม่พบคำร้องที่ตรงกับการค้นหา ต้องเพิ่มหรือเเก้ไขอย่างไรบ้าง | เเก้ไขใน DashboardPage | ไปเเก้ไขเพิ่มตรง B2 ด้านล่างใน DashboardPage |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |


*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ภูมิพัฒน์ วงศ์ดาว
